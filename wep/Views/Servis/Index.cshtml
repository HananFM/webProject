@model IEnumerable<wep.Models.Servis>

@{
	ViewData["Title"] = "Index";
}

<h1>Index</h1>
<div class="alert-danger"> @TempData["msj"]</div>
@if (User.IsInRole("admin"))
{
	<p>
		<a asp-action="Create">Create New</a>
	</p>
}
<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.ServisName)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.ServisFee)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.employee)
			</th>
			@if (User.IsInRole("admin"))
			{
				<th></th>
			}
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.ServisName)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.ServisFee)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.employee.EmployeeName)
				</td>
				@if (User.IsInRole("admin"))
				{
					<td>
						<a asp-action="Edit" asp-route-id="@item.ServisID">Edit</a> |
						<a asp-action="Details" asp-route-id="@item.ServisID">Details</a> |
						<a asp-action="Delete" asp-route-id="@item.ServisID">Delete</a>
					</td>
				}
			</tr>
		}
	</tbody>
</table>
