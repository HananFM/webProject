@model IEnumerable<wep.Models.Rendezvou>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RandezvouTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@if (Model != null){ 
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RandezvouTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RendezvouID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RendezvouID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RendezvouID">Delete</a>
            </td>
        </tr>
    }
}
    </tbody>
</table>
